{
  "name": "autable-docs",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vitepress dev src --host 127.0.0.1",
    "build": "vitepress build src && touch docs/.nojekyll",
    "preview": "vitepress preview src --host 127.0.0.1"
  },
  "devDependencies": {
    "vitepress": "1.6.4"
  },
  "overrides": {
    "vite": "6.4.3"
  }
}
